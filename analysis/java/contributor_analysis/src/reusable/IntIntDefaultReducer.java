package reusable;


import org.apache.hadoop.io.*;
import org.apache.hadoop.mapreduce.Reducer;

public class IntIntDefaultReducer extends Reducer<IntWritable, IntWritable, IntWritable, IntWritable> {
	
}